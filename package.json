{
  "name": "vse-graphql-insights",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "BROWSER=none react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "prettier": "prettier --write 'src/**/*.js'",
    "deploy": "yarn build && surge -p ./build --domain vse-4it445-graphql-insights.surge.sh"
  },
  "dependencies": {
    "react": "16.12.0",
    "react-dom": "16.12.0",
    "spectacle-scripts": "2.4.0"
  },
  "devDependencies": {
    "prettier": "1.19.1"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ]
}
